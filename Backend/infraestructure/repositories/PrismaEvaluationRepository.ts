// Infrastructure repository implementation placeholder (Prisma)
export class PrismaEvaluationRepository {
  // prisma client would be injected here in real implementation
  async save(evaluation: any) {
    // TODO: persist with prisma
    return evaluation;
  }
  async findById(id: string) {
    // TODO: query prisma
    return null;
  }
}
